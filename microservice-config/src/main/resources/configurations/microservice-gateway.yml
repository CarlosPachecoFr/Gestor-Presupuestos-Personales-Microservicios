server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      globalcors:
        cors-configurations:
          '[/**]':
           allowedOrigins: "http://localhost:4200"
           allowedMethods: ["GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH"]
           allowedHeaders: ["*"]
      routes:
        - id: usuario
          uri: lb://microservice-usuario
          predicates:
            - Path=/gpp/usuario/**
        - id: auth
          uri: lb://microservice-auth
          predicates:
            - Path=/gpp/auth/**
        - id: transaccion
          uri: lb://microservice-transacciones
          predicates:
            - Path=/gpp/transaccion/**
        - id: meta
          uri: lb://microservice-metas
          predicates:
            - Path=/gpp/metas/**

eureka:
  instance:
    hostname: microservice-eureka
  client:
    service-url:
      defaultZone: http://microservice-eureka:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "health,info,gateway"